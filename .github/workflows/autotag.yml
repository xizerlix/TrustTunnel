# Upstream uses AdGuardSoftwareLimited/native-libs-common (not available on forks).

name: Check CHANGELOG and auto-tag

on:
  workflow_dispatch:

jobs:
  upstream-only:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled on fork
        run: echo "Auto-tag runs only on AdGuardSoftwareLimited/trusttunnel-endpoint upstream."
