# Upstream uses AdGuardSoftwareLimited/native-libs-common (not available on forks).

name: Create release PR

on:
  workflow_dispatch:

jobs:
  upstream-only:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled on fork
        run: echo "Release PR workflow runs only on AdGuardSoftwareLimited/trusttunnel-endpoint upstream."
