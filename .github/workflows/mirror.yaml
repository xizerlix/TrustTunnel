# Upstream uses AdGuardSoftwareLimited/actions (not available on forks).
# This stub replaces the reusable-workflow call so tag/branch pushes stay green.

name: Mirror to public repository

on:
  workflow_dispatch:

jobs:
  upstream-only:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled on fork
        run: echo "Mirror runs only on AdGuardSoftwareLimited/trusttunnel-endpoint upstream."
